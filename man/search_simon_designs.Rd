% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_simon.R
\name{search_simon_designs}
\alias{search_simon_designs}
\title{Search for Simon two-stage designs meeting target error rates}
\usage{
search_simon_designs(p0, pa, alpha = 0.05, power = 0.8, n_max = 100)
}
\arguments{
\item{p0}{Numeric. Response rate under the null hypothesis.}

\item{pa}{Numeric. Response rate under the alternative (\code{pa > p0}).}

\item{alpha}{Numeric. Maximum type I error (one-sided). Default 0.05.}

\item{power}{Numeric. Minimum power. Default 0.80.}

\item{n_max}{Integer. Largest total sample size to search. Default 100.}
}
\value{
A tibble of class \code{simon_search} with one row per candidate total
sample size and columns \code{n1}, \code{r1}, \code{n}, \code{r}, \code{type1_error}, \code{power},
\code{pet_null}, \code{expected_n_null}, and \code{criterion} (\code{"optimal"},
\code{"minimax"}, both, or \code{NA}).
}
\description{
Searches over stage sizes and boundaries for Simon (1989) two-stage
designs with type I error at most \code{alpha} and power at least \code{power}.
For each total sample size \code{n}, the design minimizing the expected sample
size under the null is retained; the globally best design is flagged
\code{"optimal"} and the design with the smallest maximum sample size is
flagged \code{"minimax"}.

Search and construction are separate steps: this function returns a
tibble of candidates, and \code{\link[=pick_design]{pick_design()}} (or \code{\link[=design_simon]{design_simon()}} with any
row's parameters) turns a candidate into a design object.
}
\examples{
candidates <- search_simon_designs(p0 = 0.05, pa = 0.25, n_max = 30)
candidates
pick_design(candidates, "optimal")

}
\references{
Simon, R. (1989). Optimal two-stage designs for phase II clinical trials.
\emph{Controlled Clinical Trials}, 10(1), 1-10.
\doi{10.1016/0197-2456(89)90015-9}
}
\seealso{
\code{\link[=pick_design]{pick_design()}}, \code{\link[=design_simon]{design_simon()}}
}
