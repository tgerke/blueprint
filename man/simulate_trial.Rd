% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/simulate_trial.R
\name{simulate_trial}
\alias{simulate_trial}
\alias{simulate_trial.two_stage_design}
\alias{simulate_trial.single_stage_design}
\alias{simulate_trial.basket_trial_design}
\title{Simulate trials from a design}
\usage{
simulate_trial(design, ...)

\method{simulate_trial}{two_stage_design}(design, ...)

\method{simulate_trial}{single_stage_design}(design, ...)

\method{simulate_trial}{basket_trial_design}(design, ...)
}
\arguments{
\item{design}{A \code{trial_design} object (or a legacy blueprint design).}

\item{...}{Method-specific arguments; see the method documentation.}
}
\value{
Method-specific; tidy design classes return a tibble with one row
per simulated trial.
}
\description{
Simulates trial realizations from a design object under a data-generating
scenario. Methods exist for each blueprint design class; new design
families plug in by providing a method.

For the modern design classes (e.g., \code{\link[=design_simon]{design_simon()}}), pass a \code{scenario}
created with \code{\link[=assume_response]{assume_response()}}, \code{\link[=assume_null]{assume_null()}}, or
\code{\link[=assume_alternative]{assume_alternative()}} via the \code{under} argument, and the result is a tidy
tibble with one row per simulated trial. Legacy classes
(\code{two_stage_design}, \code{single_stage_design}, \code{basket_trial_design}) retain
their original arguments (e.g., \code{shape}, \code{scale}, \code{p_true}) and return
structure.
}
\section{Methods (by class)}{
\itemize{
\item \code{simulate_trial(two_stage_design)}: Legacy two-stage TTE designs; delegates to
\code{\link[=simulate_two_stage_tte]{simulate_two_stage_tte()}} (pass \code{shape} and \code{scale}).

\item \code{simulate_trial(single_stage_design)}: Legacy single-stage TTE designs; delegates to
\code{\link[=simulate_single_stage_tte]{simulate_single_stage_tte()}} (pass \code{shape} and \code{scale}).

\item \code{simulate_trial(basket_trial_design)}: Legacy Bayesian basket designs; delegates to
\code{\link[=simulate_basket_trial]{simulate_basket_trial()}} (pass \code{p_true} and optionally
\code{allocation_probs}).

}}
\examples{
design <- design_simon(n1 = 13, r1 = 3, n = 43, r = 12, p0 = 0.2, pa = 0.4)
simulate_trial(design, under = assume_alternative(design), n_sims = 10)

}
\seealso{
\code{\link[=evaluate]{evaluate()}} for operating characteristics, \code{\link[=verify]{verify()}} to check
simulated against exact operating characteristics.
}
