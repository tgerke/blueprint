% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_simon.R
\name{design_simon}
\alias{design_simon}
\title{Construct a Simon two-stage design}
\usage{
design_simon(
  n1,
  r1,
  n,
  r,
  p0,
  pa,
  alpha = NULL,
  power = NULL,
  criterion = NULL
)
}
\arguments{
\item{n1}{Integer. Stage 1 sample size.}

\item{r1}{Integer. Stage 1 futility boundary: stop if responses <= \code{r1}.}

\item{n}{Integer. Total sample size across both stages.}

\item{r}{Integer. Final boundary: reject the null if responses > \code{r}.}

\item{p0}{Numeric. Response rate under the null hypothesis.}

\item{pa}{Numeric. Response rate under the alternative hypothesis
(\code{pa > p0}).}

\item{alpha}{Optional numeric. The type I error target the design was
selected to meet (metadata; the attained error is computed exactly).}

\item{power}{Optional numeric. The power target the design was selected
to meet (metadata; the attained power is computed exactly).}

\item{criterion}{Optional character, \code{"optimal"} or \code{"minimax"}, recording
how the design was selected.}
}
\value{
An object of class \code{c("simon_design", "trial_design")}.
}
\description{
Constructs a Simon (1989) two-stage design for a single-arm trial with a
binary endpoint from its four structural parameters. Use this when the
design is already known (e.g., reproducing a design from a protocol);
use \code{\link[=search_simon_designs]{search_simon_designs()}} to find designs meeting target error rates.

The design: enroll \code{n1} patients in stage 1 and stop for futility if \code{r1}
or fewer respond. Otherwise enroll \code{n - n1} more patients and reject the
null hypothesis if more than \code{r} of the \code{n} total patients respond.
}
\examples{
# The classic p0 = 0.2 vs. pa = 0.4 optimal design
design <- design_simon(n1 = 13, r1 = 3, n = 43, r = 12, p0 = 0.2, pa = 0.4)
design
evaluate(design)

}
\references{
Simon, R. (1989). Optimal two-stage designs for phase II clinical trials.
\emph{Controlled Clinical Trials}, 10(1), 1-10.
\doi{10.1016/0197-2456(89)90015-9}
}
\seealso{
\code{\link[=search_simon_designs]{search_simon_designs()}}, \code{\link[=evaluate]{evaluate()}}, \code{\link[=verify]{verify()}},
\code{\link[=simulate_trial]{simulate_trial()}}, \code{\link[=draft_protocol_text]{draft_protocol_text()}}
}
