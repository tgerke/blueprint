% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{evaluate}
\alias{evaluate}
\title{Evaluate operating characteristics of a design}
\usage{
evaluate(design, ...)
}
\arguments{
\item{design}{A \code{trial_design} object.}

\item{...}{Method-specific arguments. Common ones:
\describe{
\item{under}{A \code{scenario} (or named list of scenarios). Defaults to
the design's own null and alternative hypotheses.}
\item{method}{\code{"exact"} or \code{"simulation"}.}
\item{n_sims}{Number of simulated trials when \code{method = "simulation"}.}
}}
}
\value{
A tibble with one row per scenario and columns for the operating
characteristics (e.g., \code{prob_success}, \code{prob_early_stop}, \code{expected_n}).
}
\description{
Computes the operating characteristics of a design under one or more
data-generating scenarios, returning a tidy tibble. Where a design family
supports it (e.g., Simon two-stage), characteristics are available in
closed form (\code{method = "exact"}); simulation is always available
(\code{method = "simulation"}).
}
\examples{
design <- design_simon(n1 = 13, r1 = 3, n = 43, r = 12, p0 = 0.2, pa = 0.4)
evaluate(design)
evaluate(design, under = assume_response(0.3))

}
\seealso{
\code{\link[=verify]{verify()}} to compare exact and simulated results.
}
